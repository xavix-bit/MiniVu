# 本地优先说明

MiniVu 的 OCR、识图问答和对话都在这台 Mac 上完成。图片、识别文字、问题和回答不会发给云端模型。

MiniVu 只在你主动执行这些操作时联网：

- 下载模型或安装可选组件；
- 测试下载源速度；
- 检查模型或运行时更新。

这些操作可能访问 ModelScope、Hugging Face、PyPI 或 GitHub，具体取决于下载源和所选后端。

框选截图会先写入系统临时目录，读取后立即删除。OCR 如需临时文件，也会在读取后删除。临时会话关闭后不会默认保存。

只有主动导出会把 Markdown 和图片写入你选择的目录。导出文件由你保留和管理。
